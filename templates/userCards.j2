{% extends "base.j2" %}
{% block content %}
<h1 style="margin-top: 2rem; color: red; text-align: center;font-size: 5rem; text-shadow: 0 0 5px black, 0 0 5px black;" class="cf">{{ user }}'s Collection</h1>
{% include "userCardsJS.j2" %}


<div class="tab">
	<button id="Full_Collection" class="mainbtn tablinks">Full Collection</button>
	<button id="Monsters" class="mainbtn tablinks">Monsters</button>
	<button id="Items" class="mainbtn tablinks">Items</button>
	<button id="Base_Set" class="mainbtn tablinks">Monster Base Set</button>
	<button id="Item_Base_Set" class="mainbtn tablinks">Item Base Set</button>
	<button id="Heroes" class="mainbtn tablinks">Heroes</button>
	<button id="Promos" class="mainbtn tablinks">Promos</button>
	<br>
	<div id="msets">
	{% for set in msets %}
	<button class="tablinks" id="{{ set[0].replace('?','9').replace("'",'8').replace(" ",'_') }}">{{ set[0] }}</button>
	{% endfor %}
	</div>
	<div id="isets">
	{% for set in isets %}
	<button class="tablinks" id="{{ set[0].replace('?','9').replace("'",'8').replace(" ",'_') }}">{{ set[0] }}</button>
	{% endfor %}
	</div>
</div>

<h2>Cards<h2>

<table id="cs" class="display tbl" width="100%">
    <thead>
	<tr>
		<th>CardID</th>
        	<th>CR</th>
        	<th>Card</th>
        	<th>Expansion</th>
        	<th>Grade</th>
        	<th>Holo</th>
        	<th>Edition</th>
        	<th>Value</th>
        	<th>Set</th>
	</tr>
    </thead>
</table>

<table id=setTable class="display tbl" width="100%">
	<thead>
		<tr>
                        <th></th>
			<th>Card</th>
			<th>Aquired?</th>
			<th>Quantity</th>
		</tr>
	</thead>
</table>
{% endblock %}
